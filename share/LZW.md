```ts
minCodeSize // 最小长度 取值范围 5～8
codeSize // 6～12
const clearCode = 1 << minCodeSize // 清除编译表标记，值是原始数据长度加1，就是256/1024/4096
const eoiCode = clearCode + 1 // 编译结束标记，值是清除码+1

output // 结果输出表
dict // 字典表

readCode //
/**
 * 1. 参数 size ，取多少位的值作为 code 码
 * 2. 源数据的每个字符当成是 8 位的二进制，比如 170, 171 就当成 '10101010', '10101011'
 * 3. 取 size 位数的二进制合并成一个数字，比如 size 是 9，那么就取 101010101, 即 341 
 */

/**
 * 1. 循环读取数据
 * 2. 如果 code 码是清除码，清除字典表
 * 3. 如果 code 码是结束码，输出结果
 * 4. 
 */
```
